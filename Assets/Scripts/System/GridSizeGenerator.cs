using System;
using System.Collections.Generic;

namespace MatchGame.Utils
{
    //public static class GridSizeGenerator
    //{
    //    public static Grid GetGridSizeForLevel(int level)
    //    {
          
    //            int rows, columns;
    //        // Calculate the dimensions of the puzzle based on the level number.
    //        if (level < 2)
    //        {
    //            rows = columns = level * 2;
    //        }
    //        else
    //        {
    //            rows = (level/2) * 2 +1;  // Increase rows.
    //            columns = (level/2-1)  * 2+2;  // Increase columns.
    //        }

    //        // Ensure that row * column size is divisible by 2.
    //        if ((rows * columns) % 2 != 0)
    //        {
    //            rows++;
    //        }
    //        if (level / 2 != 0)
    //        {
    //            return new Grid(columns, rows);
    //        }
    //        else
    //            return  new Grid(rows, columns);
          
    //    }
    //}

}